services:
  nextjs-prod:
    build:
      context: ../../
      dockerfile: ./docker/nextjs/Dockerfile
      target: prod
    ports:
      - 4000:3000
    environment:
      - BACKEND_HOST=http://nestjs-prod:3000
      - JWT_SECRET=${JWT_SECRET:?error}
    env_file:
      - .env
    profiles:
      - production
  nextjs-dev:
    build:
      context: ../../
      dockerfile: ./docker/nextjs/Dockerfile
      target: dev
    ports:
      - 4000:3000
    environment:
      - BACKEND_HOST=http://nestjs-dev:3000
      - JWT_SECRET=${JWT_SECRET:?error}
    env_file:
      - .env
    profiles:
      - development
    develop:
      watch:
        - action: sync
          path: ../../
          target: /usr/local/app
          ignore:
            - crawlers
            - docker
            - nest-cli.json
